# 🚀 Пошаговая инструкция по деплою на GitHub Pages

## Шаг 1: Подготовка репозитория на GitHub

### 1.1 Создайте репозиторий на GitHub

1. Откройте [GitHub](https://github.com)
2. Нажмите **"New repository"** (зелёная кнопка)
3. Заполните форму:
   - **Repository name**: `agent-skills-academy` (или своё название)
   - **Description**: Интерактивное обучение агентным системам Claude AI
   - **Public** (для GitHub Pages нужен публичный репозиторий на бесплатном плане)
   - ❌ НЕ ставьте галочку "Initialize this repository with a README"
4. Нажмите **"Create repository"**

### 1.2 Обновите конфигурацию проекта

**Откройте файл `vite.config.js` и измените:**

```js
base: '/agent-skills-academy/', // Замените на ВАШ repository name
```

Если ваш репозиторий называется по-другому (например `my-claude-app`):
```js
base: '/my-claude-app/',
```

**Откройте файл `package.json` и измените:**

```json
"homepage": "https://yourusername.github.io/agent-skills-academy"
```

Замените:
- `yourusername` на ваше имя пользователя GitHub
- `agent-skills-academy` на название вашего репозитория

**Пример для пользователя `alex123` с репозиторием `my-app`:**
```json
"homepage": "https://alex123.github.io/my-app"
```

### 1.3 Пересоберите проект с новыми настройками

```bash
npm run build
```

### 1.4 Закоммитьте изменения

```bash
git add .
git commit -m "fix: update GitHub Pages configuration"
```

## Шаг 2: Подключение к GitHub

### 2.1 Добавьте удалённый репозиторий

Скопируйте URL вашего репозитория со страницы GitHub (он выглядит как `https://github.com/username/repo.git`)

```bash
git remote add origin https://github.com/ВАШ_USERNAME/agent-skills-academy.git
```

**Пример:**
```bash
git remote add origin https://github.com/alex123/my-claude-app.git
```

### 2.2 Отправьте код на GitHub

```bash
git branch -M main
git push -u origin main
```

Если потребуется авторизация:
- **Username**: ваш логин GitHub
- **Password**: используйте **Personal Access Token** (НЕ пароль от аккаунта)

#### Как создать Personal Access Token:
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. **Generate new token (classic)**
3. Название: `deploy-token`
4. Срок действия: 90 days
5. Права: поставьте галочку **`repo`** (полный доступ к репозиториям)
6. **Generate token**
7. **СКОПИРУЙТЕ ТОКЕН** (он показывается только один раз!)

## Шаг 3: Деплой на GitHub Pages

### 3.1 Установите gh-pages (если ещё не установлено)

```bash
npm install -D gh-pages
```

### 3.2 Задеплойте приложение

```bash
npm run deploy
```

Эта команда:
1. Соберёт проект (`npm run build`)
2. Создаст ветку `gh-pages`
3. Загрузит содержимое папки `dist` в эту ветку
4. Отправит на GitHub

### 3.3 Настройте GitHub Pages

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Pages** (в левом меню)
3. В разделе **Source** выберите:
   - **Branch**: `gh-pages`
   - **Folder**: `/ (root)`
4. Нажмите **Save**

### 3.4 Дождитесь деплоя

GitHub начнёт процесс деплоя. Вы увидите сообщение:
> Your site is ready to be published at https://username.github.io/agent-skills-academy/

Процесс займёт 1-3 минуты.

## Шаг 4: Проверка

### 4.1 Откройте ваш сайт

Перейдите по адресу:
```
https://ваш_username.github.io/agent-skills-academy/
```

**Пример:**
```
https://alex123.github.io/my-claude-app/
```

### 4.2 Проверьте работоспособность

Убедитесь, что:
- ✅ Главная страница загружается
- ✅ Навигация работает
- ✅ Флэш-карточки переворачиваются
- ✅ Тесты работают
- ✅ Достижения отображаются
- ✅ Прогресс сохраняется в LocalStorage

## Шаг 5: Обновление сайта

Когда вы вносите изменения в код:

```bash
# 1. Внесите изменения в коде
# 2. Закоммитьте в main
git add .
git commit -m "описание изменений"
git push origin main

# 3. Задеплойте новую версию
npm run deploy
```

GitHub Pages автоматически обновит сайт через 1-2 минуты.

---

## 🔧 Решение проблем

### Проблема: Белая страница после деплоя

**Причина:** Неправильная настройка `base` в `vite.config.js`

**Решение:**
1. Проверьте, что `base` совпадает с названием репозитория:
   ```js
   base: '/ваш-repo-name/',
   ```
2. Пересоберите: `npm run build`
3. Задеплойте: `npm run deploy`

### Проблема: 404 при переходе по маршрутам

**Причина:** GitHub Pages не поддерживает client-side routing без дополнительной настройки

**Решение:** Используется `HashRouter` (уже настроен в `App.jsx`):
```jsx
import { HashRouter as Router } from 'react-router-dom';
```

Маршруты будут выглядеть так:
- `/#/`
- `/#/learning`
- `/#/achievements`

### Проблема: CSS не загружается

**Причина:** Tailwind не сконфигурирован правильно

**Решение:**
1. Убедитесь, что установлен Tailwind 3: `npm install -D tailwindcss@3`
2. Проверьте `tailwind.config.js`
3. Пересоберите проект

### Проблема: Ошибка авторизации при push

**Причина:** Используете пароль вместо токена

**Решение:**
1. Создайте Personal Access Token (см. Шаг 2.2)
2. Используйте токен вместо пароля при авторизации

### Проблема: `npm run deploy` не работает

**Причина:** gh-pages не установлен или неправильно настроен

**Решение:**
```bash
# Переустановите gh-pages
npm uninstall gh-pages
npm install -D gh-pages

# Задеплойте снова
npm run deploy
```

---

## 📚 Полезные ссылки

- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [Vite Deployment Guide](https://vitejs.dev/guide/static-deploy.html)
- [Creating Personal Access Token](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token)

---

## ✅ Checklist перед деплоем

- [ ] Создан репозиторий на GitHub
- [ ] Обновлён `base` в `vite.config.js`
- [ ] Обновлён `homepage` в `package.json`
- [ ] Проект собирается без ошибок (`npm run build`)
- [ ] Git инициализирован и код закоммичен
- [ ] Remote origin добавлен
- [ ] Код отправлен на GitHub (`git push origin main`)
- [ ] Выполнен `npm run deploy`
- [ ] В Settings → Pages выбрана ветка `gh-pages`
- [ ] Сайт открывается по URL

---

**Если возникли проблемы, создайте issue на GitHub или проверьте логи деплоя в Actions вашего репозитория.**
